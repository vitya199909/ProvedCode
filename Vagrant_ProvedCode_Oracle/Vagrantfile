Vagrant.configure("2") do |config|
  # ---------------------------
  # Frontend VM
  # ---------------------------
  config.vm.define "frontend" do |frontend|
    frontend.vm.box = "generic/oracle8"
    frontend.vm.hostname = "frontend"
    frontend.vm.network "private_network", ip: "192.168.56.10"
    config.vm.synced_folder "./provedcode-frontend-vitya199909", "/home/vagrant/frontend", type: "rsync", rsync__exclude: ['.git/', 'node_modules/']
    frontend.vm.provision "shell", privileged: true, path: "provision-frontend.sh"
    frontend.vm.network "forwarded_port", guest: 3000, host: 3000
    frontend.vm.provider "virtualbox" do |vb|
      vb.memory = "2048"
      vb.cpus = 2
    end
  end

  # ---------------------------
  # Database VM
  # ---------------------------
  config.vm.define "db" do |db|
    db.vm.box = "generic/oracle8"
    db.vm.hostname = "db"
    db.vm.network "private_network", ip: "192.168.56.12"
    db.vm.provision "shell", path: "provision-db.sh"
    db.vm.network "forwarded_port", guest: 5432, host: 5432
    db.vm.provider "virtualbox" do |vb|
      vb.memory = "2048"
      vb.cpus = 2
    end
  end

  # ---------------------------
  # Backend VM
  # ---------------------------
  config.vm.define "backend" do |backend|
    backend.vm.box = "generic/oracle8"
    backend.vm.hostname = "backend"
    backend.vm.network "private_network", ip: "192.168.56.11"
    backend.vm.synced_folder "./provedcode-backend-vitya199909", "/home/vagrant/backend", type: "rsync", rsync__exclude: ['.git/', 'target/']
    backend.vm.provision "shell", privileged: true, path: "provision-backend.sh"
    backend.vm.network "forwarded_port", guest: 8080, host: 8080
    backend.vm.provider "virtualbox" do |vb|
      vb.memory = "2048"
      vb.cpus = 2
    end
  end
end